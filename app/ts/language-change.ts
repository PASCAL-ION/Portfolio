/* export function languageChange(data: MyData[]) {
    data.forEach((project, i) => {
      // Utilisez ici les données reçues du fetch pour effectuer le traitement nécessaire
      // Par exemple, afficher les données dans la console ou manipuler le DOM
      console.log(project.title);
      console.log(project.image);
      console.log(project.text);
      console.log(project.githubLink);
    });
  } */